The only special feature about this piece of code is that 
the return value from an if statement is used to determine
the absolute value of a or b. This allows us to construct
rich expressions.

((if (> b 0) + - ) a b)
